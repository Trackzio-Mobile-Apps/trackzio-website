/** Props passed from `getStaticProps` for app Privacy / Terms pages. */
export type LegalPageDoc = {
  title: string;
  lastUpdated: string;
  analyticsPage: string;
  analyticsEvent: string;
  html: string;
};
